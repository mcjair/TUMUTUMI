rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura autenticada o en modo demo controlado
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
